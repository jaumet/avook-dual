# Docker Compose overrides (optional)
# Leave these blank to let Docker pick random free host ports.
# Set explicit values (e.g. 8000 / 6060) when you want predictable URLs.
BACKEND_PORT=
FRONTEND_PORT=
