# Docker Compose overrides (optional)
# These defaults expose the backend on http://localhost:8000 and the
# frontend on http://localhost:6060. Change the values if those ports are
# already in use on your machine.
BACKEND_PORT=8000
FRONTEND_PORT=6060
